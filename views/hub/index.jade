extends layout

block content
    ul.display
        each project in projects
            li
                a(href="#{project.href}", class="no-style")
                    div.display(style={'background-image': 'url(#{project.image})'})
                        h3= project.name
                        p= project.description
    img#stop_spying_on_my_shit(src="/images/im_watching_you.png")
